# User Profile

Preferences, habits, and corrections learned from interactions.

- User explicitly prefers lower-risk approaches when multiple options exist.
- User is interested in MCP (Model Context Protocol) tooling as an integration pattern.
- User explicitly prefers lower-risk approaches when given options.
- User is interested in MCP as an integration pattern for external services.
- User values security-conscious design, such as expiring links for credential collection.
- User explicitly prefers cost-efficiency in background/automated processes like evolution updates.
- User prefers configurable intervals with sensible defaults rather than hardcoded behavior.
- User implicitly corrected the agent's framing by pointing out the session ID limitation the agent hadn't mentioned.
- User explicitly chose the Playwright persistent session approach over manual cookie refresh for LinkedIn automation.
- User has a casual, informal communication style and appreciates concise, practical responses.
- User prefers message age/recency to be clearly communicated when reviewing recruiter threads.