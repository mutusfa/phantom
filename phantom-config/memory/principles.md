# Principles

Distilled strategic principles from session observations.

- Consistent preference (6x): User explicitly prefers lower-risk approaches when multiple options exist.
- Consistent preference (2x): User is interested in MCP (Model Context Protocol) tooling as an integration pattern.
